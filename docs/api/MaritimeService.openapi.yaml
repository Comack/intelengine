openapi: 3.1.0
info:
    title: MaritimeService API
    version: 1.0.0
paths:
    /api/maritime/v1/get-vessel-snapshot:
        post:
            tags:
                - MaritimeService
            summary: GetVesselSnapshot
            description: GetVesselSnapshot retrieves a point-in-time view of AIS vessel traffic and disruptions.
            operationId: GetVesselSnapshot
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GetVesselSnapshotRequest'
                required: true
            responses:
                "200":
                    description: Successful response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetVesselSnapshotResponse'
                "400":
                    description: Validation error
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ValidationError'
                default:
                    description: Error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
    /api/maritime/v1/list-navigational-warnings:
        post:
            tags:
                - MaritimeService
            summary: ListNavigationalWarnings
            description: ListNavigationalWarnings retrieves active maritime safety warnings from NGA.
            operationId: ListNavigationalWarnings
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ListNavigationalWarningsRequest'
                required: true
            responses:
                "200":
                    description: Successful response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListNavigationalWarningsResponse'
                "400":
                    description: Validation error
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ValidationError'
                default:
                    description: Error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
    /api/maritime/v1/list-sar-detections:
        post:
            tags:
                - MaritimeService
            summary: ListSarDetections
            description: ListSarDetections retrieves SAR-detected dark ships from Global Fishing Watch.
            operationId: ListSarDetections
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ListSarDetectionsRequest'
                required: true
            responses:
                "200":
                    description: Successful response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListSarDetectionsResponse'
                "400":
                    description: Validation error
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ValidationError'
                default:
                    description: Error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
    /api/maritime/v1/get-port-congestion:
        post:
            tags:
                - MaritimeService
            summary: GetPortCongestion
            description: GetPortCongestion retrieves congestion indices for major global ports.
            operationId: GetPortCongestion
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/GetPortCongestionRequest'
                required: true
            responses:
                "200":
                    description: Successful response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetPortCongestionResponse'
                "400":
                    description: Validation error
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ValidationError'
                default:
                    description: Error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Error'
components:
    schemas:
        Error:
            type: object
            properties:
                message:
                    type: string
                    description: Error message (e.g., 'user not found', 'database connection failed')
            description: Error is returned when a handler encounters an error. It contains a simple error message that the developer can customize.
        FieldViolation:
            type: object
            properties:
                field:
                    type: string
                    description: The field path that failed validation (e.g., 'user.email' for nested fields). For header validation, this will be the header name (e.g., 'X-API-Key')
                description:
                    type: string
                    description: Human-readable description of the validation violation (e.g., 'must be a valid email address', 'required field missing')
            required:
                - field
                - description
            description: FieldViolation describes a single validation error for a specific field.
        ValidationError:
            type: object
            properties:
                violations:
                    type: array
                    items:
                        $ref: '#/components/schemas/FieldViolation'
                    description: List of validation violations
            required:
                - violations
            description: ValidationError is returned when request validation fails. It contains a list of field violations describing what went wrong.
        GetVesselSnapshotRequest:
            type: object
            properties:
                boundingBox:
                    $ref: '#/components/schemas/BoundingBox'
            description: GetVesselSnapshotRequest specifies filters for the vessel snapshot.
        BoundingBox:
            type: object
            properties:
                northEast:
                    $ref: '#/components/schemas/GeoCoordinates'
                southWest:
                    $ref: '#/components/schemas/GeoCoordinates'
            description: |-
                BoundingBox represents a rectangular geographic area defined by its corners.
                 Used for spatial queries to filter results within a geographic region.
        GeoCoordinates:
            type: object
            properties:
                latitude:
                    type: number
                    maximum: 90
                    minimum: -90
                    format: double
                    description: Latitude in decimal degrees (-90 to 90).
                longitude:
                    type: number
                    maximum: 180
                    minimum: -180
                    format: double
                    description: Longitude in decimal degrees (-180 to 180).
            description: GeoCoordinates represents a geographic location using WGS84 coordinates.
        GetVesselSnapshotResponse:
            type: object
            properties:
                snapshot:
                    $ref: '#/components/schemas/VesselSnapshot'
            description: GetVesselSnapshotResponse contains the vessel traffic snapshot.
        VesselSnapshot:
            type: object
            properties:
                snapshotAt:
                    type: integer
                    format: int64
                    description: 'Snapshot timestamp, as Unix epoch milliseconds.. Warning: Values > 2^53 may lose precision in JavaScript'
                densityZones:
                    type: array
                    items:
                        $ref: '#/components/schemas/AisDensityZone'
                disruptions:
                    type: array
                    items:
                        $ref: '#/components/schemas/AisDisruption'
            description: VesselSnapshot represents a point-in-time view of civilian AIS vessel data.
        AisDensityZone:
            type: object
            properties:
                id:
                    type: string
                    minLength: 1
                    description: Zone identifier.
                name:
                    type: string
                    description: Zone name (e.g., "Strait of Malacca").
                location:
                    $ref: '#/components/schemas/GeoCoordinates'
                intensity:
                    type: number
                    maximum: 100
                    minimum: 0
                    format: double
                    description: Traffic intensity score (0-100).
                deltaPct:
                    type: number
                    format: double
                    description: Change from baseline as a percentage.
                shipsPerDay:
                    type: integer
                    format: int32
                    description: Estimated ships per day.
                note:
                    type: string
                    description: Analyst note.
            required:
                - id
            description: AisDensityZone represents a zone of concentrated vessel traffic.
        AisDisruption:
            type: object
            properties:
                id:
                    type: string
                    minLength: 1
                    description: Disruption identifier.
                name:
                    type: string
                    description: Descriptive name.
                type:
                    type: string
                    enum:
                        - AIS_DISRUPTION_TYPE_UNSPECIFIED
                        - AIS_DISRUPTION_TYPE_GAP_SPIKE
                        - AIS_DISRUPTION_TYPE_CHOKEPOINT_CONGESTION
                    description: |-
                        AisDisruptionType represents the type of AIS tracking anomaly.
                         Maps to TS union: 'gap_spike' | 'chokepoint_congestion'.
                location:
                    $ref: '#/components/schemas/GeoCoordinates'
                severity:
                    type: string
                    enum:
                        - AIS_DISRUPTION_SEVERITY_UNSPECIFIED
                        - AIS_DISRUPTION_SEVERITY_LOW
                        - AIS_DISRUPTION_SEVERITY_ELEVATED
                        - AIS_DISRUPTION_SEVERITY_HIGH
                    description: AisDisruptionSeverity represents the severity of an AIS disruption.
                changePct:
                    type: number
                    format: double
                    description: Percentage change from normal.
                windowHours:
                    type: integer
                    format: int32
                    description: Analysis window in hours.
                darkShips:
                    type: integer
                    format: int32
                    description: Number of dark ships (AIS off) detected.
                vesselCount:
                    type: integer
                    format: int32
                    description: Number of vessels in the affected area.
                region:
                    type: string
                    description: Region name.
                description:
                    type: string
                    description: Human-readable description.
            required:
                - id
            description: AisDisruption represents a detected anomaly in AIS vessel tracking data.
        ListNavigationalWarningsRequest:
            type: object
            properties:
                pagination:
                    $ref: '#/components/schemas/PaginationRequest'
                area:
                    type: string
                    description: Optional area filter (e.g., "NAVAREA IV", "Persian Gulf").
            description: ListNavigationalWarningsRequest specifies filters for retrieving NGA warnings.
        PaginationRequest:
            type: object
            properties:
                pageSize:
                    type: integer
                    maximum: 100
                    minimum: 1
                    format: int32
                    description: Maximum number of items to return per page (1 to 100).
                cursor:
                    type: string
                    description: Opaque cursor for fetching the next page. Empty string for the first page.
            description: PaginationRequest specifies cursor-based pagination parameters for list endpoints.
        ListNavigationalWarningsResponse:
            type: object
            properties:
                warnings:
                    type: array
                    items:
                        $ref: '#/components/schemas/NavigationalWarning'
                pagination:
                    $ref: '#/components/schemas/PaginationResponse'
            description: ListNavigationalWarningsResponse contains navigational warnings matching the request.
        NavigationalWarning:
            type: object
            properties:
                id:
                    type: string
                    description: Warning identifier.
                title:
                    type: string
                    description: Warning title.
                text:
                    type: string
                    description: Full warning text.
                area:
                    type: string
                    description: Geographic area affected.
                location:
                    $ref: '#/components/schemas/GeoCoordinates'
                issuedAt:
                    type: integer
                    format: int64
                    description: 'Warning issue date, as Unix epoch milliseconds.. Warning: Values > 2^53 may lose precision in JavaScript'
                expiresAt:
                    type: integer
                    format: int64
                    description: 'Warning expiry date, as Unix epoch milliseconds.. Warning: Values > 2^53 may lose precision in JavaScript'
                authority:
                    type: string
                    description: Warning source authority.
            description: NavigationalWarning represents a maritime safety warning from NGA.
        PaginationResponse:
            type: object
            properties:
                nextCursor:
                    type: string
                    description: Cursor for fetching the next page. Empty string indicates no more pages.
                totalCount:
                    type: integer
                    format: int32
                    description: Total count of items matching the query, if known. Zero if the total is unknown.
            description: PaginationResponse contains pagination metadata returned alongside list results.
        ListSarDetectionsRequest:
            type: object
            properties:
                limit:
                    type: integer
                    format: int32
                    description: Maximum detections to return (default 100, max 500).
            description: ListSarDetectionsRequest filters SAR dark ship queries.
        ListSarDetectionsResponse:
            type: object
            properties:
                detections:
                    type: array
                    items:
                        $ref: '#/components/schemas/SarDarkShip'
                fetchedAt:
                    type: string
                    format: int64
                    description: Unix milliseconds when data was fetched.
            description: ListSarDetectionsResponse contains SAR-detected vessels with no AIS correlation.
        SarDarkShip:
            type: object
            properties:
                id:
                    type: string
                    description: Unique detection identifier.
                lat:
                    type: number
                    format: double
                    description: Detection latitude.
                lon:
                    type: number
                    format: double
                    description: Detection longitude.
                lengthM:
                    type: number
                    format: double
                    description: Estimated vessel length from SAR pixel size in metres.
                course:
                    type: number
                    format: double
                    description: Estimated course over ground in degrees (0–360).
                speedKnots:
                    type: number
                    format: double
                    description: Estimated speed in knots.
                aisMatched:
                    type: boolean
                    description: Whether the detection was correlated with a known AIS vessel.
                nearestAisVessel:
                    type: string
                    description: Name of the matched AIS vessel, if correlated.
                region:
                    type: string
                    description: Geographic region of the detection, e.g. "South China Sea".
                vesselClassHint:
                    type: string
                    description: 'Heuristic vessel class: tanker | cargo | fishing | unknown.'
                detectedAt:
                    type: string
                    format: int64
                    description: Unix milliseconds of the SAR pass.
                confidence:
                    type: number
                    format: double
                    description: Detection confidence score (0–1).
            description: SarDarkShip represents a vessel detected by synthetic aperture radar with no AIS match.
        GetPortCongestionRequest:
            type: object
            description: GetPortCongestionRequest has no required parameters.
        GetPortCongestionResponse:
            type: object
            properties:
                ports:
                    type: array
                    items:
                        $ref: '#/components/schemas/PortCongestionStatus'
                computedAt:
                    type: string
                    format: int64
                    description: Unix milliseconds when data was computed.
            description: GetPortCongestionResponse contains congestion indices for major global ports.
        PortCongestionStatus:
            type: object
            properties:
                portCode:
                    type: string
                    description: UN/LOCODE port code, e.g. "SGSIN".
                portName:
                    type: string
                    description: Human-readable port name, e.g. "Port of Singapore".
                lat:
                    type: number
                    format: double
                    description: Port latitude.
                lon:
                    type: number
                    format: double
                    description: Port longitude.
                congestionIndex:
                    type: number
                    format: double
                    description: Congestion index from 0 (clear) to 100 (severely congested).
                avgWaitHours:
                    type: number
                    format: double
                    description: Average vessel wait time at anchor in hours.
                vesselsAtAnchor:
                    type: integer
                    format: int32
                    description: Number of vessels currently at anchor in the port approaches.
                trend:
                    type: string
                    description: 'Congestion trend: increasing | stable | decreasing.'
                country:
                    type: string
                    description: ISO 3166-1 alpha-2 country code.
                updatedAt:
                    type: string
                    format: int64
                    description: Unix milliseconds of the latest data point.
            description: PortCongestionStatus represents the congestion level at a major port.
