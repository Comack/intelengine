// Code generated by protoc-gen-ts-client. DO NOT EDIT.
// source: worldmonitor/evidence/v1/service.proto

export interface IngestEvidenceRequest {
  sourceUrl: string;
  title: string;
  rawContent: string;
}

export interface IngestEvidenceResponse {
  evidence?: Evidence;
}

export interface Evidence {
  id: string;
  sourceUrl: string;
  title: string;
  rawContent: string;
  scrapedAt: number;
  extractedPole?: POLEGraph;
  isProcessed: boolean;
}

export interface POLEGraph {
  persons: POLEPerson[];
  objects: POLEObject[];
  locations: POLELocation[];
  events: POLEEvent[];
}

export interface POLEPerson {
  id: string;
  name: string;
  aliases: string[];
  role: string;
}

export interface POLEObject {
  id: string;
  type: string;
  name: string;
  description: string;
}

export interface POLELocation {
  id: string;
  name: string;
  lat: number;
  lon: number;
  countryCode: string;
}

export interface POLEEvent {
  id: string;
  type: string;
  description: string;
  timestamp: number;
  involvedPersonIds: string[];
  involvedObjectIds: string[];
  locationIds: string[];
}

export interface GetEvidenceRequest {
  id: string;
}

export interface GetEvidenceResponse {
  evidence?: Evidence;
}

export interface ListEvidenceRequest {
  limit: number;
  nextToken: string;
}

export interface ListEvidenceResponse {
  evidence: Evidence[];
  nextToken: string;
}

export interface FieldViolation {
  field: string;
  description: string;
}

export class ValidationError extends Error {
  violations: FieldViolation[];

  constructor(violations: FieldViolation[]) {
    super("Validation failed");
    this.name = "ValidationError";
    this.violations = violations;
  }
}

export class ApiError extends Error {
  statusCode: number;
  body: string;

  constructor(statusCode: number, message: string, body: string) {
    super(message);
    this.name = "ApiError";
    this.statusCode = statusCode;
    this.body = body;
  }
}

export interface EvidenceServiceClientOptions {
  fetch?: typeof fetch;
  defaultHeaders?: Record<string, string>;
}

export interface EvidenceServiceCallOptions {
  headers?: Record<string, string>;
  signal?: AbortSignal;
}

export class EvidenceServiceClient {
  private baseURL: string;
  private fetchFn: typeof fetch;
  private defaultHeaders: Record<string, string>;

  constructor(baseURL: string, options?: EvidenceServiceClientOptions) {
    this.baseURL = baseURL.replace(/\/+$/, "");
    this.fetchFn = options?.fetch ?? globalThis.fetch;
    this.defaultHeaders = { ...options?.defaultHeaders };
  }

  async ingestEvidence(req: IngestEvidenceRequest, options?: EvidenceServiceCallOptions): Promise<IngestEvidenceResponse> {
    let path = "/api/evidence/v1/ingest-evidence";
    const url = this.baseURL + path;

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "POST",
      headers,
      body: JSON.stringify(req),
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as IngestEvidenceResponse;
  }

  async getEvidence(req: GetEvidenceRequest, options?: EvidenceServiceCallOptions): Promise<GetEvidenceResponse> {
    let path = "/api/evidence/v1/get-evidence";
    const url = this.baseURL + path;

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "POST",
      headers,
      body: JSON.stringify(req),
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as GetEvidenceResponse;
  }

  async listEvidence(req: ListEvidenceRequest, options?: EvidenceServiceCallOptions): Promise<ListEvidenceResponse> {
    let path = "/api/evidence/v1/list-evidence";
    const url = this.baseURL + path;

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "POST",
      headers,
      body: JSON.stringify(req),
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as ListEvidenceResponse;
  }

  private async handleError(resp: Response): Promise<never> {
    const body = await resp.text();
    if (resp.status === 400) {
      try {
        const parsed = JSON.parse(body);
        if (parsed.violations) {
          throw new ValidationError(parsed.violations);
        }
      } catch (e) {
        if (e instanceof ValidationError) throw e;
      }
    }
    throw new ApiError(resp.status, `Request failed with status ${resp.status}`, body);
  }
}

