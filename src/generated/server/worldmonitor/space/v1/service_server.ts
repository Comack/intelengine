// Code generated by protoc-gen-ts-server. DO NOT EDIT.
// source: worldmonitor/space/v1/service.proto

export interface ListSatellitesRequest {
  category: SatelliteCategory;
  limit: number;
}

export interface ListSatellitesResponse {
  satellites: Satellite[];
  propagatedAt: string;
}

export interface Satellite {
  id: string;
  name: string;
  category: SatelliteCategory;
  country: string;
  lat: number;
  lon: number;
  altitudeKm: number;
  inclinationDeg: number;
  epochMs: string;
  tleLine1: string;
  tleLine2: string;
}

export interface GetSpaceWeatherRequest {
}

export interface GetSpaceWeatherResponse {
  status?: SpaceWeatherStatus;
}

export interface SpaceWeatherStatus {
  kpIndex: number;
  kpLevel: string;
  xrayClass: string;
  xrayFlux: number;
  auroraLatNorth: number;
  auroraLatSouth: number;
  alerts: SpaceWeatherAlert[];
  observedAt: string;
}

export interface SpaceWeatherAlert {
  type: string;
  severity: string;
  message: string;
}

export type SatelliteCategory = "SATELLITE_CATEGORY_UNSPECIFIED" | "SATELLITE_CATEGORY_MILITARY" | "SATELLITE_CATEGORY_RECONNAISSANCE" | "SATELLITE_CATEGORY_COMMUNICATIONS" | "SATELLITE_CATEGORY_NAVIGATION" | "SATELLITE_CATEGORY_WEATHER" | "SATELLITE_CATEGORY_STARLINK" | "SATELLITE_CATEGORY_ISS";

export interface FieldViolation {
  field: string;
  description: string;
}

export class ValidationError extends Error {
  violations: FieldViolation[];

  constructor(violations: FieldViolation[]) {
    super("Validation failed");
    this.name = "ValidationError";
    this.violations = violations;
  }
}

export class ApiError extends Error {
  statusCode: number;
  body: string;

  constructor(statusCode: number, message: string, body: string) {
    super(message);
    this.name = "ApiError";
    this.statusCode = statusCode;
    this.body = body;
  }
}

export interface ServerContext {
  request: Request;
  pathParams: Record<string, string>;
  headers: Record<string, string>;
}

export interface ServerOptions {
  onError?: (error: unknown, req: Request) => Response | Promise<Response>;
  validateRequest?: (methodName: string, body: unknown) => FieldViolation[] | undefined;
}

export interface RouteDescriptor {
  method: string;
  path: string;
  handler: (req: Request) => Promise<Response>;
}

export interface SpaceServiceHandler {
  listSatellites(ctx: ServerContext, req: ListSatellitesRequest): Promise<ListSatellitesResponse>;
  getSpaceWeather(ctx: ServerContext, req: GetSpaceWeatherRequest): Promise<GetSpaceWeatherResponse>;
}

export function createSpaceServiceRoutes(
  handler: SpaceServiceHandler,
  options?: ServerOptions,
): RouteDescriptor[] {
  return [
    {
      method: "POST",
      path: "/api/space/v1/list-satellites",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const body = await req.json() as ListSatellitesRequest;
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("listSatellites", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.listSatellites(ctx, body);
          return new Response(JSON.stringify(result as ListSatellitesResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "POST",
      path: "/api/space/v1/get-space-weather",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const body = await req.json() as GetSpaceWeatherRequest;
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("getSpaceWeather", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.getSpaceWeather(ctx, body);
          return new Response(JSON.stringify(result as GetSpaceWeatherResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
  ];
}

