// Code generated by protoc-gen-ts-server. DO NOT EDIT.
// source: worldmonitor/evidence/v1/service.proto

export interface IngestEvidenceRequest {
  sourceUrl: string;
  title: string;
  rawContent: string;
}

export interface IngestEvidenceResponse {
  evidence?: Evidence;
}

export interface Evidence {
  id: string;
  sourceUrl: string;
  title: string;
  rawContent: string;
  scrapedAt: number;
  extractedPole?: POLEGraph;
  isProcessed: boolean;
}

export interface POLEGraph {
  persons: POLEPerson[];
  objects: POLEObject[];
  locations: POLELocation[];
  events: POLEEvent[];
}

export interface POLEPerson {
  id: string;
  name: string;
  aliases: string[];
  role: string;
}

export interface POLEObject {
  id: string;
  type: string;
  name: string;
  description: string;
}

export interface POLELocation {
  id: string;
  name: string;
  lat: number;
  lon: number;
  countryCode: string;
}

export interface POLEEvent {
  id: string;
  type: string;
  description: string;
  timestamp: number;
  involvedPersonIds: string[];
  involvedObjectIds: string[];
  locationIds: string[];
}

export interface GetEvidenceRequest {
  id: string;
}

export interface GetEvidenceResponse {
  evidence?: Evidence;
}

export interface ListEvidenceRequest {
  limit: number;
  nextToken: string;
}

export interface ListEvidenceResponse {
  evidence: Evidence[];
  nextToken: string;
}

export interface FieldViolation {
  field: string;
  description: string;
}

export class ValidationError extends Error {
  violations: FieldViolation[];

  constructor(violations: FieldViolation[]) {
    super("Validation failed");
    this.name = "ValidationError";
    this.violations = violations;
  }
}

export class ApiError extends Error {
  statusCode: number;
  body: string;

  constructor(statusCode: number, message: string, body: string) {
    super(message);
    this.name = "ApiError";
    this.statusCode = statusCode;
    this.body = body;
  }
}

export interface ServerContext {
  request: Request;
  pathParams: Record<string, string>;
  headers: Record<string, string>;
}

export interface ServerOptions {
  onError?: (error: unknown, req: Request) => Response | Promise<Response>;
  validateRequest?: (methodName: string, body: unknown) => FieldViolation[] | undefined;
}

export interface RouteDescriptor {
  method: string;
  path: string;
  handler: (req: Request) => Promise<Response>;
}

export interface EvidenceServiceHandler {
  ingestEvidence(ctx: ServerContext, req: IngestEvidenceRequest): Promise<IngestEvidenceResponse>;
  getEvidence(ctx: ServerContext, req: GetEvidenceRequest): Promise<GetEvidenceResponse>;
  listEvidence(ctx: ServerContext, req: ListEvidenceRequest): Promise<ListEvidenceResponse>;
}

export function createEvidenceServiceRoutes(
  handler: EvidenceServiceHandler,
  options?: ServerOptions,
): RouteDescriptor[] {
  return [
    {
      method: "POST",
      path: "/api/evidence/v1/ingest-evidence",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const body = await req.json() as IngestEvidenceRequest;
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("ingestEvidence", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.ingestEvidence(ctx, body);
          return new Response(JSON.stringify(result as IngestEvidenceResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "POST",
      path: "/api/evidence/v1/get-evidence",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const body = await req.json() as GetEvidenceRequest;
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("getEvidence", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.getEvidence(ctx, body);
          return new Response(JSON.stringify(result as GetEvidenceResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "POST",
      path: "/api/evidence/v1/list-evidence",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const body = await req.json() as ListEvidenceRequest;
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("listEvidence", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.listEvidence(ctx, body);
          return new Response(JSON.stringify(result as ListEvidenceResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
  ];
}

